const reducer = function (state, action) {
    switch(action.type){
        case 'ADDTODO':
            return state;
        case 'REMOVETODO':
            return state;
        default:
            return state;
    }
}

export default reducer;