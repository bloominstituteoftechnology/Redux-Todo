
const initialState = {
    todo: [
        
    ],
};

export default (state = initialState, action) => {
    switch (action.type) {
        case :
        return {
            todo: st
        }
    }
}