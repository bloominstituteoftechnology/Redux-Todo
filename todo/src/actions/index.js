
//action component holds all kinds of actions defined in here. 
//gets state type and activates that function. returns state type to reducer.

export const handleInputValue =() => {
    return(
        type: ADD_TODO,
    ) 
}