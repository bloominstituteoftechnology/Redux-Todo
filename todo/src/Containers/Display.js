import React from 'react';

const Display = () => {
    return (
        <div>
            <input type="text" />
            <button type="submit">Submit</button>
        </div>
    );
};

export default Display;