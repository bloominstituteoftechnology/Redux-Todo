const initialState = {
  todos: ["My Todo", "Another Todo"]
};

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
