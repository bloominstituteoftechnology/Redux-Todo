import { 
  // INCREMENT 
} from '../actions';

export default (initialState = '', action) => {
  switch (action.type) {
    // case INCREMENT:
    //   return ++action.count
    default:
      return count;
  }
};
