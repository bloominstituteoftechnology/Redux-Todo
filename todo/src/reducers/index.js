const initialState = {
    todos: []
}

export default (state = initialState, action) => {
    switch (actio.tpye) {
        case:
            return {}
        case:
            return {}
        default:
            return state;    
    }
}