// ActionType Exports


// Initial State 
const initialState = {
    todoList: []
}

// Reducer
const reducer = (state = initialState, action) => {
    switch(action.type){

        default: 
            return state;
    }
}

export default reducer;