import { ADD_TODO, DEL_TODO } from '../actions';

export default (todo, action) => {
    switch (action.type) {
      case ADD_TODO:
        return ;
      case DEL_TODO:
        return ;
      default:
        return ;
    }
  };