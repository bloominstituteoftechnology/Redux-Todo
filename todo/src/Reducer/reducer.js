

const initialState = {
    todos: [
        {
            value: 'Initial State',
            complete: false
        }
    ]
}

export default (state = initialState, action) => {
    return state
}