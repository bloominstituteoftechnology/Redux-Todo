import React from 'react';

const Link = ({ ActiveXObject, children, onClick}) => {
    <button 
        onClick = {onClick}
        disabled = {disabled}
        style = {{
            marginLeft: '4px'
        }} >
        {children}
        </button>
}

export default Link;